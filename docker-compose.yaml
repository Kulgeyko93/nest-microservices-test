version: "3"
services:
  reservations:
    build:
      context: .
      dockerfile: ./apps/reservations/Dockerfile
      target: development
    command: yarn dev-reservations
    ports:
      - '7001:7001'
    volumes:
      - .:/opt/src/app
  mongodb:
    image: mongo
